// to install a npm package locally as a dependency
// --> npm install  <package_name> --save OR npm i  <package_name>  --save

// to install a npm package locally as a dev-dependency(for development only)
// --> npm install  <package_name> --save-dev OR npm i <package_name> --save-dev

// to install a package globally 
// --> npm install -g nodemon